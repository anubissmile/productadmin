import { ResponseMessageProduct } from './response-message-product';

describe('ResponseMessageProduct', () => {
  it('should create an instance', () => {
    expect(new ResponseMessageProduct()).toBeTruthy();
  });
});
