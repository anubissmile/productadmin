import { ResponseMessageUser } from './response-message-user';

describe('ResponseMessageUser', () => {
  it('should create an instance', () => {
    expect(new ResponseMessageUser()).toBeTruthy();
  });
});
